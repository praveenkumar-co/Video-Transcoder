apiVersion: v1
kind: Service
metadata:
  name: prometheus
  namespace: default
spec:
  ports:
  - port: 9090
    targetPort: 9090
    nodePort: 30900
    protocol: TCP
  selector:
    app: prometheus
  type: NodePort
